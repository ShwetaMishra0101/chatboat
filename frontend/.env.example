# Base URL of the deployed backend API (no trailing slash).
# Local dev leaves this unset and falls back to http://localhost:5050/api.
# In Vercel, set this to your backend deployment, e.g.:
VITE_API_URL=https://your-backend.vercel.app/api
